#!/bin/bash
if [ $(hostname) = "debwork" ] ; then
	xrandr --output DVI-I-1 --primary --preferred \
		--output VGA-0 --preferred --right-of DVI-I-1
elif [ $(hostname) = "ssdeb" ] ; then
	true # FIXME
fi
